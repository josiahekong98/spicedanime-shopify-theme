{% liquid
  assign family_url = url | default: fallback_url
  assign family_available = available | default: false
  assign family_href = family_url
  if family_href contains '?'
    assign family_href = family_href | append: '&filter.p.m.custom.series=' | append: series_filter
  else
    assign family_href = family_href | append: '?filter.p.m.custom.series=' | append: series_filter
  endif
%}

<a class="sa-series-family-card{% unless family_available or image != blank %} sa-series-family-card--quiet{% endunless %}" href="{{ family_href }}" role="listitem" aria-label="Shop {{ title | escape }}">
  <span class="sa-series-family-card__media">
    {% if image != blank %}
      <img src="{{ image | image_url: width: 560 }}" alt="{{ title | escape }}">
    {% else %}
      <span class="sa-series-family-card__fallback">Explore</span>
    {% endif %}
  </span>
  <span class="sa-series-family-card__label">{{ title | escape }}</span>
</a>
